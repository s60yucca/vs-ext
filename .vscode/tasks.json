{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build claude-code-model-mapper",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/claude-code-model-mapper"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$tsc"
      ],
      "detail": "Compile the Claude Code Model Mapper extension"
    }
  ]
}
